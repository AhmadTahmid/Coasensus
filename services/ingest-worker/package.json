{
  "name": "@coasensus/ingest-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build && node dist/index.js",
    "smoke": "npm run build && node dist/smoke.js",
    "api": "npm run build && node dist/feed-api.js",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --pool=threads"
  },
  "dependencies": {
    "@coasensus/filter-engine": "file:../filter-engine",
    "@coasensus/shared-types": "file:../../packages/shared-types"
  }
}
